namespace CommunityManager.Models
{
    public class GlobalVars
    {
        //This overrides roles, allowing all widgets to be visible regardless of role type
        //ONLY set to true for testing; when published, this should be false
        public static bool ShowAll = false;
    }
}
